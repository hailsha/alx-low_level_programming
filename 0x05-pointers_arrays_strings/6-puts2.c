#include "main.h"

/**
 * puts2 - print every other character of a string.
 * @str: character
 */
void puts2(char *str)
{
	int i = 0;

	while (*s != '\0')
	{
		if (i % 2 = 0)
			_putchar(*s);
		s++;
		i++;
	}
	_pucthar('\n');
}
